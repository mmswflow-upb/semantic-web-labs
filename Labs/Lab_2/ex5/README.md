# Exercise 5 — XPath against `dvd.xml`

The DVD catalogue already lives at `Lab_2/dvd.xml`. Run the XPath expressions listed in `queries.txt` against that file using `xmllint`, `xidel`, or any XPath-aware tool (the instructions mention `https://videlibri.sourceforge.net/cgi-bin/xidelcgi`).

Each entry in `queries.txt` includes the task brief, the XPath expression, and the expected output generated from the existing XML.
